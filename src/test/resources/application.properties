# 테스트에서는 seed 적재를 끈다 — DataInitializer(@ConditionalOnProperty)가 비활성화돼
# bgl_log/bgl_template/pattern_view seed가 @SpringBootTest 컨텍스트를 오염시키지 않는다.
# (seed 서비스 단위 테스트는 서비스를 직접 호출하므로 이 설정과 무관)
app.seed.enabled=false
